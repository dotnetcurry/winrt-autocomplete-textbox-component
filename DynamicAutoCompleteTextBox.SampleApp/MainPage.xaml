<Page
    x:Class="DynamicAutoCompleteTextBox.SampleApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DynamicAutoCompleteTextBox.SampleApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dtb="using:DynamicAutoCompleteTextBox"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="140"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Left" 
                   Style="{StaticResource PageHeaderTextStyle}" 
                   Margin="120,54,0,0" 
                   TextWrapping="Wrap" 
                   Text="Dynamic AutoComplete TextBox Sample" 
                   VerticalAlignment="Top" 
                   Height="68" Width="1003"/>
        <dtb:AutoCompleteTextBox x:Name="DynamicAutoCompleteTextControl" 
                                 Grid.Row="1" 
                                 Margin="120,20,120,20"
                                 Text=""
                                 MenuTrigger="@">
        </dtb:AutoCompleteTextBox>
    </Grid>
</Page>
